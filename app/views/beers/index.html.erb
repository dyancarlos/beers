<% @beers.each do |beer| %>
	
	<div id="each_beer">
		<%= image_tag("garrafa.png", :width => "76") %>
		<p><%= beer.name %></p>
	</div>
	<%#= link_to "ja tomei", add_my_beers_path(:user_id => current_user.uid, :beer_id => beer.id) %>
	
<% end %>